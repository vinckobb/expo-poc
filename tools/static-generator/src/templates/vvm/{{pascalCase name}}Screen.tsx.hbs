import { View, Text, Button, SafeAreaView } from "react-native";
import { {{pascalCase name}}ViewModel } from "./{{pascalCase name}}ViewModel";

export function {{pascalCase name}}Screen({ viewModel }: { viewModel: {{pascalCase name}}ViewModel }) {
  return (
    <SafeAreaView>
      <View>
        <Text
        {{{{raw}}}}
          style={{
            fontSize: 32,
            fontWeight: "bold",
            textAlign: "center",
            marginVertical: 24,
          }}
        {{{{/raw}}}}
        >
          O aplikácii
        </Text>
        <Button title="Home" onPress={() => viewModel.onHomeButton()} />
      </View>
    </SafeAreaView>
  );
}
