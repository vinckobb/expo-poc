import { NavigationProp } from "@react-navigation/native";

import * as FlowType from "./types/flowTypes";

export class {{pascalCase name}}FlowRouterImpl<T extends FlowType.ParamList>
  implements FlowType.Router
{
  static readonly displayName: string = "{{pascalCase name}}FlowRouterImpl";

  constructor(
    private nav: NavigationProp<T>,
    private delegate?: FlowType.Delegate
  ) {}
  
  openDemo() {
    console.log("Navigating to demo");
    const params: FlowType.ParamList[typeof FlowType.Screens.DEMO] = {
      id: "demoId",
    };
    this.nav.navigate(FlowType.Screens.DEMO, params);
  }

  openHome() {
    console.log("Navigating to home via delegate");
    this.delegate?.openHome();
  }
}
