import * as Flow from "./types/flowTypes";
import { DemoAction } from "../screens";

export class {{pascalCase name}}FlowControllerImpl implements Flow.Controller {
  static readonly displayName: string = "{{pascalCase name}}FlowControllerImpl";

  constructor(private router: Flow.Router) {}

  handleDemoAction(action: DemoAction) {
    switch (action.type) {
      case "home":
        this.router.openHome();
        break;
    }
  }

  // Lifecycle methods
  dispose() {
    console.log("❌ {{pascalCase name}} flow disposed");
  }
}
