import * as FlowType from "./types/flowTypes";

export class {{pascalCase name}}FlowControllerImpl implements FlowType.Controller {
  static readonly displayName: string = "{{pascalCase name}}FlowControllerImpl";

  constructor(private router: FlowType.Router) {}

  handleDemoAction(action: FlowType.Actions.DemoAction) {
    switch (action.type) {
      case "home":
        this.router.openHome();
        break;
    }
  }

  // Lifecycle methods
  dispose() {
    console.log("❌ {{pascalCase name}} flow disposed");
  }
}
